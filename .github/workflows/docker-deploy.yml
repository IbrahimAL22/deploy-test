name: CI/CD Pipeline

on:
  push:
    branches: [ main ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # Checkout the repository
      - uses: actions/checkout@v3

      # Set up Docker Buildx
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      # Log in to GitHub Container Registry
      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # Build and push the Docker image
      - name: Build and push Docker image
        uses: docker/build-push-action@v4
        with:
          context: .
          push: true
          # Replace "yourusername" with your GitHub username or organization name
          tags: ghcr.io/test:latest

      # Deploy to VPS using credentials directly (insecure)
      - name: Deploy to VPS via systemd using password
        uses: appleboy/ssh-action@v0.1.6
        with:
          host: "164.92.204.82"
          username: "root"
          password: "A8z#K9v*Qp!X3t@L6w"
          script: |
            sudo systemctl restart flask_app.service
